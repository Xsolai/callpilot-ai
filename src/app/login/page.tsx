import type {Metadata} from 'next';import {Auth} from '@/components/flows';export const metadata:Metadata={title:'Sign in',robots:{index:false,follow:false}};export default function Page(){return <Auth mode="login"/>}
